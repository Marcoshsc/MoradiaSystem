const ContractPage = () => {
  return (
    <div>
      {/* <CardSale isContract isEdit={false} />
      <CardSale isContract isEdit={false} />
      <CardSale isContract isEdit={false} /> */}
    </div>
  );
};

export default ContractPage;
